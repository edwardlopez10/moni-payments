meta {
  name: Rotate Payment Account Credentials
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/organizations/{{organizationId}}/payment-accounts/{{paymentAccountId}}/credentials/rotate
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
  Idempotency-Key: bruno-acct-rotate-1
  Content-Type: application/json
}

body:json {
  {
    "credentials": {
      "apiKey": "dev-fake-api-key-rotated",
      "webhookSecret": "dev-fake-webhook-secret-rotated"
    }
  }
}
